# AGENTS 核心准则

> **全局最高优先级规则**：本文件为项目 AI 协作唯一强制契约，所有编码、重构、迭代、文档行为必须严格遵守。

> **心智文档优先级规则**：源码为唯一真实标准，**文档滞后则同步修正.ai中文档，禁止脑补旧文档约束**。

## 1. 核心思维与行为红线
- **第一性原理与深度思考**: 面对复杂需求或 Bug，禁止生搬硬套过往经验。必须剥离表象，回归基础事实进行逻辑推导，**放慢分析步伐，禁止跳跃式决策**。
- **无痕执行与安全红线**:
  - 生成的内容与 Artifacts 必须使用**中文**。
  - 涉及销毁容器、清理网络、丢弃未追踪文件（如 `git reset --hard` / `git clean -fd`）等破坏性操作前，**必须向用户说明影响并获批**。

## 2. 编码与架构规范

### 2.1 思考后编码

- **明确表达你的假设**，如果不确定，**问，不要猜**。
- **如果用户输入存在多种解读**，不明确，需提出来，不要默默选择一种。
- **如果有更简单的方法**，请说明。必要时反驳用户。
- **如果有什么不清楚的地方**，说出困惑的地方，反问用户。

### 2.2 简洁优先
只需最小限度的代码来解决问题，不要有任何猜测性内容。

- 坚持"少即是多"，代码避免过度设计。
- 不要实现用户未要求的功能。
- 不要为只使用一次的代码引入抽象。
- 不对不可能发生的情况引入错误处理。
- 如果你写了 200 行代码，但实际上可以使用 50 行实现，那就重写。

### 2.3 精准修改
在编辑现有代码时：
- 不要"改进"相邻的代码、注释或格式。
- 不要重构没有问题的代码。
- 匹配现有风格，哪怕你个人偏好不同。
- 如果发现无关的废弃代码，**提及但不要删除**。

当修改产生孤立代码时：
- 移除**因你的修改**而变得无用的导入、变量或函数。

### 2.4 目标驱动执行
将任务转化为可验证的目标：

| 用户说 | 转化为 |
|--------|--------|
| "添加验证" | "为无效输入编写测试，然后让测试通过" |
| "修复漏洞" | "编写一个能复现该漏洞的测试，然后让测试通过" |
| "重构 X" | "确保重构前后测试均能通过" |

对于多步骤任务，列出简要计划：
```
1. [步骤] → 验证: [检查]
2. [步骤] → 验证: [检查]
3. [步骤] → 验证: [检查]
```

### 2.5 架构与工程规范
- **架构整洁**:
  - **职责单一**与**自解释**: 函数只做一件事，优先保证人类可读性，禁止取巧写法。
  - **最少依赖**与**显式错误**: 优先使用标准库，必须显式捕获并处理异常。
- **审查与追溯**: 代码审查严禁仅看局部。必须完整追溯调用链及多方调用上下文后再下结论。
- **迭代策略**: 选用能够完整满足当前需求的最简实现。避免前瞻性抽象、冗余配置与间接调用。分层迭代构建系统。先实现可端到端正常运行的最小可用版本，再在已稳定可用的成品之上新增功能。绝不以可用产品换取尚未完工的复杂方案。
- **模块化**: 组件保持模块化，职责边界清晰隔离。若成熟、维护活跃的库可以降低整体复杂度或提升可靠性，则优先选用。若无明确理由，请勿重复实现通用功能。
- **依赖管理**: 在自研实现或新增依赖包前，优先复用项目已引入的依赖。未经查阅文档与类型定义，不得默认某个库不具备所需能力。
- **长期视角**: 架构决策需具备长期视角。不接受仅临时可用、后续计划替换的临时兜底方案。
- **Commit 规范**：使用客观、精准、工程化措辞，禁止主观夸张描述。
- **代码扫描 (Diff)**: 提炼变更特性时，**绝对禁止**仅看 `git log/status`。必须通过完整差异文件（如 `git diff -U0` 导出）深度审阅，挖掘底层重构，确保 100% 无死角不遗漏

## 3. AI 认知加载机制

无需次次全量加载，精准区分「轻量任务」和「重磅任务」
### 全量加载.ai 心智库场景
- 新会话、会话重置、上下文清空
- 新增需求、功能迭代、跨模块修改、架构重构
- 对项目约束、架构、逻辑存在模糊、不确定场景
- 出现设计分歧、与原有项目逻辑冲突场景

### 免加载轻量场景（复用上下文即可）
微小 Bug 修复、文本调整、局部参数修改、简单格式优化

## 4. AI心智文档联动规则

项目五大心智文档与开发行为**强绑定、强制更新**，杜绝文档与代码脱节。

|开发行为|对应心智文档|更新要求|
|---|---|---|
|项目目标/范围/需求变更|.ai/project_goal.md|更新项目定位、核心目标、业务范围、非功能约束|
|架构分层、模块职责、目录重构|.ai/architecture.md|更新架构图、数据流、模块边界、依赖关系|
|技术选型、方案取舍、踩坑修复、风险变更|.ai/constraints.md|记录决策理由、历史问题、风险模块、禁止规范|
|新增/修改 API、数据模型、字段结构|.ai/api-contract.md|同步更新接口契约、入参出参、数据表结构、模型定义|
|迭代推进、功能完成、任务阻塞|.ai/roadmap.md|更新里程碑状态、待办、进行中、阻塞项|

### 更新触发原则
- 仅当本次变更确实触及上表某行「开发行为」时，才更新对应文档；无实质变更禁止更新，禁止为了更新而更新。
- 轻量任务（微小 Bug 修复、文本调整、参数修改）不触发任何 .ai 写入。

## 5. 输出风格（i-have-adhd 常开）

读者有 ADHD。所有回复按以下方式塑形，使其可被立即行动（源自 `.codebuddy/skills/i-have-adhd`）：

1. **先给下一步动作**：首行就是命令、路径或可执行的下一步，不铺垫背景。
2. **多步任务编号**：每步一个边界清晰的动作，步骤尽可能少。
3. **结尾给一个具体下一步**：两分钟内可完成的一件事。
4. **抑制题外话**：先解决当前问题，新问题作为独立提问留到最后。
5. **每轮重述状态**：如"第 3/5 步完成：schema 已更新"。
6. **给出具体时间估计**：用"约 15 分钟"而非"稍微改一下"。
7. **让进展可见**：说明现在什么能用了，不把成果埋在总结里。
8. **客观陈述错误**：说明位置、原因、修法，不戏剧化。
9. **列表最多 5 项**：超出则拆分"现在做/稍后"。
10. **无前缀、无回顾、无结尾客套**：禁"好的！""希望有帮助""还有什么需要？"

例外：用户要求"解释/展开"时充分说明；破坏性操作前先确认；连续三次修复失败则停下来指出可疑假设；需求歧义时只问一个简短问题。关闭：用户说"stop adhd mode"或"normal mode"。