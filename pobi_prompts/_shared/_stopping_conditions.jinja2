## 停止条件

满足以下任一条件时，即停止并返回结果：

### 成功条件（立即停止）
✅ **目标达成**：主要目标已完成（发现漏洞、获取 flag、提取数据）
✅ **高置信度**：置信度评分 ≥ 0.8（证据充分，可进入验证）

### 资源限制（停止并返回）
⚠️ **达到工具上限**：未达成目标但 10 次工具调用已耗尽
⚠️ **无实质进展**：在约 10 次尝试后，结果、置信度或发现均无实质变化或改善——停止并返回当前状态
  - **实质变化**指：发现新端点、发现新漏洞、置信度提升、提取新数据，或朝目标取得进展
  - **无实质进展**指：结果相同、置信度相同、无新发现、未朝目标推进
  - **关键要求**：不要无限期继续——约 10 次尝试仍无改善，即返回当前状态
⚠️ **无进展**：同一工具结果连续 3 次相同
⚠️ **低置信度**：置信度跌破 0.2（方法错误，需要转向）

### 错误条件（停止并上报）
❌ **重复失败**：同一工具连续 3 次失败（工具不可用，换方法）
❌ **受阻**：目标返回 403/429/限流（防御机制生效，停止测试）
❌ **结构性受阻（停止并终止任务）**：同一前置步骤连续失败，且失败根因属于**框架/工具能力缺陷**而非目标防御方主动对抗。典型信号：
  - 工具参数无法被序列化/执行（如嵌套 JSON 步骤被当作字符串）
  - 登录/认证流程反复 `success=false`，且 `final_url` 始终停在同一登录页
  - 目标表单含每次刷新的动态 CSRF token，但当前无手段抓取回填
  - 基础设施缺失（如依赖的挂载/服务未就绪，导致 agent 完全失效）
  → 判定为「当前框架无法完成」，立即停止，返回 `status: "aborted"`，并在 `detailed_summary` 中写明受阻根因交人工介入。**不要无限重试，也不要把「换方法重试」伪装成新任务继续消耗资源**。

### 停止时的处理

**若目标达成** → 返回 `status: "achieved"`、置信度 ≥ 0.8、完整发现
**若触及资源限制** → 返回 `status: "in_progress"`、当前发现、用于续做的 next_steps
**若触发错误条件** → 返回 `status: "failed"`、错误详情、替代方案
**若结构性受阻** → 返回 `status: "aborted"`、受阻根因（框架/工具缺陷）、人工介入建议

**绝不要**在超过限制后继续期待更好结果——返回当前状态交由路由决策。
