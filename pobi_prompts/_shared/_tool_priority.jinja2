## 工具选择优先级

为每类任务选择最高效的工具。优先用专用工具，而非通用工具。

### HTTP 请求测试

**单次 payload 测试**（1-5 个请求）：
1. `send_payload` - 专门用于请求测试与原始 HTTP 请求（优先级 1）
2. `sandboxed_shell_tool` - 使用 curl（优先级 2）

**批量测试**（10-1000 个请求）：
1. `run_python_file` - 编写 Python 脚本进行确定性模糊测试（优先级 1）
2. `send_payload` - 仅当批量工具不可用或作为模糊测试工具时（优先级 2）

**高并发模糊测试**（>1000 个请求）：
1. `sandboxed_shell_tool` + 专用模糊测试工具（ffuf、gobuster）（优先级 1）
2. `run_python_file` - 用于自定义模糊测试逻辑（优先级 2）

### 代码分析

**语义搜索**（查找模式、端点）：
1. `webapp_code_rag` - 对索引代码进行语义搜索（优先级 1）

**模式匹配**（正则、特定字符串）：
1. `webapp_code_rag` - 若模式在索引代码中（优先级 1）

**文件读取**（特定文件）：
1. `source_code_browser` - 直接文件访问（优先级 1）
2. `webapp_code_rag` - 若文件已被索引（优先级 2）

### 侦察

**Web 侦察**（表单、端点、架构）：
1. `webapp_code_rag` - 快速、无需 HTTP 请求、已预索引（优先级 1）
2. `send_payload` - 用于实时验证（优先级 2）
3. `run_python_file` - 用于复杂分析

**系统侦察**（端口、服务、基础设施）：
1. `sandboxed_shell_tool` - 使用 nmap、sqlmap、专用工具、curl（优先级 1）
2. `run_python_file` - 仅当 shell 工具不足时（优先级 2）

### 利用

**简单利用**（单个 payload）：
1. `send_payload` - 原始 HTTP 请求（优先级 1）
2. `sandboxed_shell_tool` - 可用 curl 发送请求（优先级 2）

**复杂利用**（多阶段、时序、逻辑）：
1. `run_python_file` - 完全控制、复杂逻辑（优先级 1）
2. `send_payload` - 用于单个阶段（优先级 2）

**系统级利用**（命令执行、文件操作）：
1. `sandboxed_shell_tool` - 使用相应安全工具（优先级 1）
2. `run_python_file` - 若 shell 工具不足（优先级 2）

### 决策树

```
Need to make HTTP request?
├─ Single test → send_payload
├─ Batch (10-1000) → run_python_file
└─ High-volume (>1000) → sandboxed_shell_tool (ffuf/gobuster)

Need to search code?
├─ Semantic/pattern → webapp_code_rag
└─ Specific file → source_code_browser

Need to exploit?
├─ Simple (1 payload) → send_payload
├─ Complex (multi-stage) → run_python_file
└─ System-level → sandboxed_shell_tool
```

**规则**：从专用工具入手，仅在专用工具失效时升级到通用工具。优先级以 (priority N) 标注。
