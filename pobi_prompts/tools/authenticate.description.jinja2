{% raw %}
通过真实浏览器（Pydoll / Chrome DevTools Protocol）对当前目标执行认证，并将得到的 cookie、浏览器存储和派生请求头**持久化**为一个可复用的 ``AuthContext`` profile。本工具成功之后，其他工具（``browser_run_steps``、``pw_send_payload``）可通过传入 ``auth_profile=<profile>`` 复用已保存的认证。

## 何时使用

- 目标需要认证，且上下文中已**发现**登录 URL / 表单。
- OAuth / OIDC 流程需要真实浏览器（重定向、回调、JS 水合）。
- 需要弹窗 / 新窗口式 OAuth 流程（``window.open(...)``）。
- 需要在子 agent 与工具间共享一个持久的已认证上下文。

## 何时不使用

- 目标没有登录（只读 / 公开）。
- 你只需要单次原始 HTTP 请求 —— 用 ``pw_send_payload``。
- 你尚未真正发现真实的登录 URL 或选择器 —— 先去发现。

## 流程分发

工具根据 ``auth_flow`` 选择实现方式：

* ``form`` / ``oauth`` / ``authorization_code`` / ``callback`` / *未设置* →
  真实浏览器流程（Pydoll）。使用 ``steps``、``popup_steps``、``success_*``
  与 ``callback_*`` 参数。
* ``json`` → 通过 HTTP 的 API 登录（无浏览器）。使用 ``request_method``、
  ``request_headers``、``request_body``（含 ``{{username}}`` /
  ``{{password}}`` 占位符）、``token_path``、``token_header_name``、
  ``token_header_format``、``capture_cookies``。
* ``http`` → HTTP Basic 认证。只需提供 ``profile``，以及可选的
  探测用 ``auth_url``（如 ``/admin``）。

## 输入约定（工具参数）

### 通用
| Argument | Type | Meaning |
|---|---|---|
| ``auth_url`` | string \| null | 登录 URL。``form``/``oauth`` 下为要打开的页面；``json`` 下为 API 端点；``http`` 下为可选的探测 URL。若省略，则使用钱包中的 ``login_url``。 |
| ``profile`` | string | 使用的凭据 profile（默认 ``"default"``）。当钱包中存在该 profile 时，使用其凭据；你也可以新起一个 profile 名，将发现/用户提供的凭据保存于此。 |
| ``auth_flow`` | enum \| null | 取值之一：``http``、``form``、``json``、``oauth``、``authorization_code``、``callback``。 |
| ``auth_type`` | enum \| null | 取值之一：``session_cookie``、``bearer_token``、``api_key``、``oauth2``。 |
| ``username`` | string \| null | 显式的用户名覆盖。仅用于上下文中发现的凭据（如默认的 ``admin:admin``、源码泄露、README）或用户直接提供的凭据。本次调用优先于钱包。 |
| ``password`` | string \| null | 显式的密码覆盖。规则同 ``username``。 |

### 仅浏览器流程
| ``steps`` | array | 在**主标签页**执行的 ``BrowserStep`` 列表（与 ``browser_run_steps`` 同结构）。 |
| ``context`` | object | 步骤可能通过 ``key`` 引用的额外非机密值。``username`` / ``password`` 由钱包自动注入 —— **绝不要**在此放入真实凭据。 |
| ``expects_popup`` | bool | 若点击认证按钮会打开弹窗 / 新窗口（如 Google/GitHub OAuth），为 ``true``。 |
| ``popup_steps`` | array | 弹窗标签页出现后，在其中执行的 ``BrowserStep`` 列表。 |
| ``callback_url_contains`` | string \| null | 基于重定向的认证完成后，**主标签页** URL 中应包含的子串（如 ``/oauth/callback``）。 |
| ``popup_callback_url_contains`` | string \| null | 弹窗关闭前其 URL 中应包含的子串。 |
| ``wait_for_popup_close`` | bool | 若为 ``true``，在快照主标签页前先等待弹窗关闭。 |
| ``success_url_contains`` | string \| null | 证明登录成功的最终 URL 子串（如 ``/dashboard``）。 |
| ``success_selector`` | string \| null | 登录后应出现的 CSS 选择器（如 ``a[href='/logout']``）。 |
| ``success_cookie_names`` | array | 登录后应出现的 cookie 名（如 ``["sessionid"]``）。 |
| ``success_storage_keys`` | array | 应出现的 localStorage / sessionStorage 键（如 ``["access_token"]``）。 |
| ``post_login_wait_ms`` | int | 成功条件匹配后的额外等待，供 SPA 令牌水合使用。默认 1500 ms。 |
| ``verify_ssl`` | bool | 若为 ``false``，忽略 TLS 错误。 |
| ``headless`` | bool | 无头运行浏览器（默认 ``true``）。 |

选择器必须来自你真正见过的真实 HTML —— 绝不猜测。

### 仅 JSON 流程
| Argument | Type | Meaning |
|---|---|---|
| ``request_method`` | string | HTTP 方法，默认 ``"POST"``。 |
| ``request_headers`` | object | 可选的额外请求头；当 ``request_body`` 为 JSON 对象时会自动加上 ``Content-Type: application/json``。 |
| ``request_body`` | any | 可 JSON 序列化的请求体。其中的字符串支持 ``{{username}}`` / ``{{password}}`` / ``{{login_url}}`` 占位符，由钱包填充。 |
| ``token_path`` | string \| null | 从响应 JSON 中提取令牌的点路径，如 ``access_token`` 或 ``data.tokens.0.value``。 |
| ``token_header_name`` | string | 保存令牌的请求头名。默认 ``"Authorization"``。 |
| ``token_header_format`` | string | 含 ``{token}`` 的格式串。默认 ``"Bearer {token}"``。 |
| ``capture_cookies`` | bool | 是否也保存 ``Set-Cookie`` cookie。默认 ``true``。 |

### 仅 Basic 流程
无需额外参数。钱包的 ``username`` + ``password`` 会构建 ``Authorization: Basic <b64>``。若希望工具先探测再保存，可提供 ``auth_url``。

## 凭据处理

凭据可能来自以下两种来源之一，按此优先级顺序：

1. **显式的 ``username`` / ``password`` 工具参数** —— 本次调用优先。在以下情况使用：
   * 用户在对话中直接键入了凭据。
   * agent 在目标上下文中发现了凭据（如注释中的默认 ``admin:admin``、泄露的密钥、源码、响应体、README）。
2. **所选 ``profile`` 的钱包凭据** —— 未提供显式参数时使用。模型不可见。

工具体内的引用规则：

- 在浏览器步骤中，通过 ``key`` 引用它们：``{"action":"fill","selector":"input[name=user]","key":"username"}``。合并后的用户名/密码已预填充到这些 key 下。
- 在 JSON 请求体中，使用 ``{{username}}`` / ``{{password}}`` 占位符。若已设置显式参数则解析为显式值，否则解析为钱包值。
- 在 HTTP Basic 中无需占位符：工具用生效的用户名/密码构建 ``Authorization: Basic <b64>``。

### 安全说明

- 通过 ``username`` / ``password`` 传入的任何内容**都会**出现在 agent 的工作上下文窗口中。钱包凭据不会。**只要目标存在对应 profile，就优先使用钱包。**
- 绝不要编造凭据。只提供在上下文中真正发现、或用户显式给出的凭据。
- 磁盘上保存的 AuthContext 可能包含得到的令牌/cookie；这是有意为之。工具的*返回值*绝不**包含 cookie/令牌/密码值。

## 输出（仅安全元数据）

成功运行返回类似如下 JSON：

```json
{
  "success": true,
  "auth_context_saved": true,
  "profile": "default",
  "target": "http://localhost:8080",
  "target_slug": "localhost_8080",
  "auth_flow": "form",
  "auth_type": "session_cookie",
  "final_url": "http://localhost:8080/dashboard",
  "page_title": "Dashboard",
  "cookies_count": 3,
  "cookie_names": ["sessionid"],
  "storage_keys": ["access_token"],
  "headers_available": ["Authorization"],
  "success_matched": ["url", "selector"]
}
```

cookie 值、令牌与密码**绝不**返回。其他 agent 应在 ``browser_run_steps`` / ``pw_send_payload`` 上使用 ``auth_profile="<profile>"`` 来消费已保存的认证。

## 示例

### JSON / API 登录（返回 JWT）

```json
{
  "auth_url": "http://target.local/api/auth/login",
  "profile": "default",
  "auth_flow": "json",
  "auth_type": "bearer_token",
  "request_method": "POST",
  "request_headers": {"X-App": "pobi"},
  "request_body": {
    "email": "{{username}}",
    "password": "{{password}}"
  },
  "token_path": "access_token",
  "token_header_name": "Authorization",
  "token_header_format": "Bearer {token}",
  "capture_cookies": true
}
```

### HTTP Basic 认证（无登录表单）

```json
{
  "profile": "admin",
  "auth_flow": "http",
  "auth_type": "api_key",
  "auth_url": "http://target.local/admin"
}
```

### 用户提供凭据的表单登录

```json
{
  "auth_url": "http://target.local/login",
  "profile": "user_supplied",
  "auth_flow": "form",
  "auth_type": "session_cookie",
  "username": "alice@example.com",
  "password": "hunter2",
  "steps": [
    {"action": "fill", "selector": "input[name='email']", "key": "username"},
    {"action": "fill", "selector": "input[name='password']", "key": "password"},
    {"action": "click", "selector": "button[type='submit']"}
  ],
  "success_url_contains": "/dashboard"
}
```

### 从已发现的 admin 端点做默认凭据测试

```json
{
  "profile": "discovered_admin",
  "auth_flow": "http",
  "auth_type": "api_key",
  "auth_url": "http://target.local/admin",
  "username": "admin",
  "password": "admin"
}
```

### 经典表单登录

```json
{
  "auth_url": "http://target.local/login",
  "profile": "default",
  "auth_flow": "form",
  "auth_type": "session_cookie",
  "steps": [
    {"action": "fill", "selector": "input[name='username']", "key": "username"},
    {"action": "fill", "selector": "input[name='password']", "key": "password"},
    {"action": "click", "selector": "button[type='submit']"}
  ],
  "success_url_contains": "/dashboard",
  "success_selector": "a[href='/logout']"
}
```

### SPA / JWT 登录

```json
{
  "auth_url": "http://target.local/login",
  "profile": "default",
  "auth_flow": "form",
  "auth_type": "bearer_token",
  "steps": [
    {"action": "fill", "selector": "#email", "key": "username"},
    {"action": "fill", "selector": "#password", "key": "password"},
    {"action": "click", "selector": "#sign-in"}
  ],
  "success_storage_keys": ["access_token"],
  "post_login_wait_ms": 2000
}
```

### 弹窗式 OAuth

```json
{
  "auth_url": "http://target.local/login",
  "profile": "default",
  "auth_flow": "oauth",
  "auth_type": "session_cookie",
  "expects_popup": true,
  "steps": [
    {"action": "click", "selector": "button.login-with-oauth"}
  ],
  "popup_steps": [
    {"action": "fill", "selector": "input[type='email']", "key": "username"},
    {"action": "click", "selector": "#identifierNext"},
    {"action": "fill", "selector": "input[type='password']", "key": "password"},
    {"action": "click", "selector": "#passwordNext"}
  ],
  "popup_callback_url_contains": "/oauth/callback",
  "wait_for_popup_close": true,
  "success_selector": "a[href='/logout']"
}
```

## 规则

- 调用本工具**之前**先发现登录 URL 与选择器。不要编造端点。
- 用 ``profile`` 选择凭据。绝不内嵌真实机密。
- 至少配置一个成功信号（``success_url_contains``、``success_selector``、``success_cookie_names`` 或 ``success_storage_keys``）；否则工具仅依赖“未抛出异常”。
- 成功运行后，下游工具应以 ``auth_profile="<profile>"`` 调用，以测试已认证行为；以 ``auth_profile=null`` 调用则与未认证基线做对比。
{% endraw %}
