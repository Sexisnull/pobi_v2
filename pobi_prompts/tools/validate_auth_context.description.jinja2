探测一个已保存的 ``AuthContext``（由 ``profile`` 选定），以确认其仍处于已认证状态，并将判定结果写入已保存上下文的元数据。在一段明显间隔后让其他 agent 消费某个 ``auth_profile`` 之前，或每当下游工具报告结构化的 "auth expired" 错误时，都应使用本工具。

JWT bearer 令牌会在任何网络调用之前，通过 ``exp`` 声明在本地短路判断（零开销）。对于不透明令牌 / 基于 cookie 的会话，则发送一次 GET 探测。

## 何时使用

- 在 ``authenticate`` 成功之后，记录一个 ``validation_url`` 并锁定 "validated=true" 的判定，以便其他 agent 选择加入该 profile。
- 在一段较长的空闲（> 几分钟）之后，发起新一轮已认证请求之前。
- 当下游工具返回 ``{"expired": true, "expired_reason": "..."}`` 时。

## 何时不使用

- 在 ``authenticate`` 已经返回 success_matched 证据之后 —— 已保存的上下文已经过验证。
- 对每个请求：``pw_send_payload`` 与 ``browser_run_steps`` 已经基于一个短时 TTL 缓存自动验证。

## 输入约定

| Argument | Type | Meaning |
|---|---|---|
| ``profile`` | string | 要验证的 profile，默认 ``"default"``。 |
| ``validation_url`` | string \| null | 要探测的已认证端点（如 ``/api/me``、``/profile``）。**传入从上下文中发现的显式 URL。** 若省略，回退到 ``metadata['validation_url']``，再回退到 ``metadata['final_url']``。 |
| ``expected_status`` | array \| null | 视为已认证的状态码（默认 ``[200..207]``）。 |
| ``failure_status`` | array \| null | 明确意味着已过期的状态码（默认 ``[401, 403]``）。 |
| ``success_substring`` | string \| null | 响应体中必须出现的标记（如 ``"logout"``、``"profile"``）。当应用到期时返回 200 + 登录页时很有用。 |
| ``success_jsonpath`` | string \| null | 必须能解析的 JSON 响应中的点路径（如 ``user.id``）。 |
| ``skip_jwt_exp_shortcut`` | bool | 即便 JWT 在本地看起来已过期也强制发起网络探测。默认 ``false``。 |
| ``jwt_exp_leeway_s`` | int | JWT exp 检查的时钟偏移容忍秒数。默认 ``0``。 |
| ``verify_ssl`` | bool | TLS 验证。默认 ``false``。 |
| ``timeout_ms`` | number \| null | 探测超时（毫秒）。默认 15000。 |

## 输出（仅安全元数据）

```json
{
  "success": true,                    // == validated
  "validated": true,
  "expired": false,
  "expired_reason": null,             // 或 "jwt_exp" | "http_401" | "missing_substring" | ...
  "validation_url": "http://target.local/api/me",
  "last_validation_status": 200,
  "last_validated_at": "2026-05-10T...",
  "profile": "default",
  "headers_available": ["Authorization"],
  "cookie_names": ["sessionid"],
  "storage_keys": ["access_token"],
  "jwt_exp": 1736000000               // 仅当我们能读取 JWT exp 声明时
}
```

cookie 值、令牌值与密码**绝不**返回。

## 示例

### 验证 JWT bearer 会话

```json
{
  "profile": "default",
  "validation_url": "http://target.local/api/me",
  "success_jsonpath": "user.id"
}
```

### 针对到期返回登录表单的页面验证 cookie 会话

```json
{
  "profile": "default",
  "validation_url": "http://target.local/dashboard",
  "success_substring": "Sign out"
}
```

## 规则

- 始终传入在上下文中发现的真实 ``validation_url``。不要猜测端点。
- 至少配置一个强信号：显式状态码、``success_substring`` 或 ``success_jsonpath``。默认 200-OK 规则会被那些以 200 返回登录页的应用欺骗。
- 若本工具报告 ``expired=true``：接下来调用 ``refresh_auth_context``（若已知 ``refresh_url``），否则重新运行 ``authenticate``。
