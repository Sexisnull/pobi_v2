在会话日志文件中按正则表达式搜索模式（requester.jsonl 与 python_interpreter.jsonl）。

## 用法

在当前会话日志中搜索模式，以查找此前工具执行产生的历史请求、响应、令牌、错误或数据。

**参数**：
- `pattern`：Python 正则表达式（如 `"token.*[A-Za-z0-9]+"`、`"status.*200"`、`"error|Error"`）

**搜索位置**：
- `requester.jsonl`：HTTP 请求/响应数据
- `python_interpreter.jsonl`：Python 脚本执行结果

## 何时使用

- 从之前的请求中查找认证令牌、cookie、会话 ID
- 搜索特定端点、URL 或请求模式
- 提取状态码、错误消息或响应模式
- 获取 Python 脚本输出或错误
- 识别会话历史中反复出现的模式或异常

## 输出

返回格式化字符串，包含：
- 找到的匹配总数
- 每条匹配：文件名、行号、匹配文本、位置、上下文（前 200 字符）

## 示例模式

```
"token.*[A-Za-z0-9]{20,}"           # 查找认证令牌
"/api/.*"                            # 搜索 API 端点
"status.*code.*[0-9]{3}"            # 提取状态码
"error|Error|ERROR|exception"       # 查找错误消息
"password|secret|api_key"           # 查找敏感数据
"(?i)content-type.*application/json" # 大小写不敏感的请求头搜索
```

## 说明

- 默认区分大小写（用 `(?i)` 做大小写不敏感搜索）
- 作用域为会话级（只搜索当前会话日志）
- 对缺失文件做优雅处理
- 只读（不修改日志）
- 使用合法的 Python 正则语法
