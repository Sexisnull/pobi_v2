将 Python 代码写入文件并在隔离的 WebAssembly 环境中执行。将文件写入与执行合并为单次操作。

## 用法

在带有自动包管理的隔离环境中执行 Python 脚本以进行安全测试。

**参数**（全部必填）：
- `code`：要执行的 Python 源码
- `filename`：文件名（如 "exploit.py"、"fuzzer.py"）
- `packages`：所需包列表（如 `["requests", "beautifulsoup4"]`）

## 何时使用

- 执行 Python 利用脚本、payload 或分析脚本
- 批量 HTTP 请求测试（10-1000+ 个请求）
- 需要 Python 逻辑的复杂安全测试
- 自定义模糊测试、数据处理或分析脚本

## 包要求

**强制**：调用工具前，从你的代码中提取所有外部包。

- **标准库**（os、sys、json、re、urllib、itertools）→ 不要包含
- **第三方**（requests、numpy、beautifulsoup4）→ 必须包含
- 若无需外部包 → 使用空列表 `[]`

**示例**：
```python
import requests       # 第三方 → 包含
import json          # 标准库 → 跳过
from bs4 import BeautifulSoup  # 第三方 → 以 "beautifulsoup4" 包含

# packages = ["requests", "beautifulsoup4"]
```

## 特性

- 自动文件管理（保存到 `~/.cache/pobi/python/`）
- 沙箱执行（隔离的 WebAssembly 环境）
- 包安装（执行前自动安装）
- JSON 结果（含 stdout、stderr、返回值的结构化输出）
- 会话支持（通过 `_session_id` 跨运行保持状态）

## 示例

```python
run_python_file(
    code="import requests\nresponse = requests.get('https://example.com')\nprint(response.status_code)",
    filename="test.py",
    packages=["requests"]
)
```

## 说明

- 单次调用中三个参数 모두必填
- 缺失包会导致 ImportError
- 与宿主系统完全隔离
- 文件保存到 `~/.cache/pobi/python/<filename>`
- 会话 ID 可选（未提供时自动生成）
