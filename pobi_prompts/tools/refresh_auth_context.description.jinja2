{% raw %}
不重新运行浏览器流程，而用刷新端点续期一个已保存的 ``AuthContext`` profile。现有上下文**原地**更新：同一 profile、同一磁盘路径、同一 cookie（与响应中的新 cookie 合并）。仅重写 bearer 请求头与选定的元数据字段。

## 何时使用

- 在 ``validate_auth_context`` 报告 ``expired=true`` 之后，或在下游工具报告结构化的 "auth expired" 错误之后。
- 当钱包（或已保存的元数据）为该 profile 声明了 ``refresh_url``，且令牌签发方支持刷新时。

## 何时不使用

- 不存在刷新端点时 —— 改为重新运行 ``authenticate``。
- 底层凭据本身已变更（如密码轮换）时 —— 重新运行 ``authenticate``。

## 输入约定

| Argument | Type | Meaning |
|---|---|---|
| ``profile`` | string | 要刷新的 profile，默认 ``"default"``。 |
| ``refresh_url`` | string \| null | 显式的刷新端点。回退顺序为 ``metadata['refresh_url']``，然后是钱包的 ``refresh_url``。 |
| ``refresh_method`` | string | HTTP 方法，默认 ``"POST"``。 |
| ``refresh_headers`` | object \| null | 随刷新请求发送的额外请求头。支持 ``{{...}}`` 模板。 |
| ``refresh_body`` | any | 可 JSON 序列化的请求体。其中的字符串支持 ``{{username}}`` / ``{{password}}`` / ``{{access_token}}`` / ``{{refresh_token}}`` 占位符。 |
| ``new_token_path`` | string | 从响应 JSON 中提取新令牌的点路径。默认 ``"access_token"``。 |
| ``new_token_header_name`` | string | 要重写的请求头名。默认 ``"Authorization"``。 |
| ``new_token_header_format`` | string | 含 ``{token}`` 的格式串。默认 ``"Bearer {token}"``。 |
| ``capture_cookies`` | bool | 合并响应中的 ``Set-Cookie`` cookie。默认 ``true``。 |
| ``verify_ssl`` | bool | TLS 验证。默认 ``false``。 |
| ``timeout_ms`` | number \| null | 请求超时（毫秒）。默认 15000。 |

## 凭据处理

``refresh_body`` / ``refresh_headers`` 内 ``{{username}}`` / ``{{password}}`` 占位符的优先级：

1. 显式的 ``username`` / ``password`` 工具参数（用户显式提供，或在上下文中发现时使用）。
2. 所选 ``profile`` 的钱包凭据。

- 已有的 access token 与 refresh token（当存在于存储 / cookie 中时）会自动注入模板上下文，因此 ``{{access_token}}`` / ``{{refresh_token}}`` 无需显式覆盖即可使用。
- 绝不要将真实机密直接放入 ``refresh_body`` 字符串中 —— 使用占位符。
- 通过 ``username`` / ``password`` 工具参数传入的任何内容**都会**出现在 agent 的上下文窗口中。钱包凭据不会。只要 profile 存在，就优先使用钱包。

## 输出（仅安全元数据）

```json
{
  "success": true,
  "refreshed": true,
  "refresh_url": "http://target.local/api/auth/refresh",
  "last_refreshed_at": "2026-05-10T...",
  "last_refresh_status": 200,
  "validated": true,
  "new_token_obtained": true,
  "new_cookies_obtained": false,
  "profile": "default",
  "headers_available": ["Authorization"],
  "cookie_names": ["sessionid"],
  "storage_keys": ["access_token"]
}
```

cookie 值、令牌值与密码**绝不**返回。

## 示例

### 标准 OAuth 风格刷新

```json
{
  "profile": "default",
  "refresh_url": "http://target.local/api/auth/refresh",
  "refresh_body": {"refresh_token": "{{refresh_token}}"},
  "new_token_path": "access_token"
}
```

### 用自定义令牌请求头刷新

```json
{
  "profile": "default",
  "refresh_body": {"token": "{{access_token}}"},
  "new_token_path": "data.token",
  "new_token_header_name": "X-Auth-Token",
  "new_token_header_format": "{token}"
}
```

## 规则

- 仅在 ``validate_auth_context`` 报告 ``expired=true``，或钱包声明了你打算按计划执行的 ``refresh_url`` 时才调用本工具。
- 若刷新失败（HTTP ≥ 400、无令牌、无 cookie），回退到完整的 ``authenticate`` 调用。
- 绝不编造 ``refresh_url`` 取值 —— 它们必须来自上下文、钱包或已保存的元数据。
{% endraw %}
