对目标 Web 应用已索引的客户端代码进行语义搜索。

## 返回内容

- **端点**：API URL、路由、fetch/XHR/axios 调用、表单 action
- **架构**：页面结构、路由逻辑、组件组织
- **表单**：输入字段、参数名、校验规则、类型

## 何时使用

- 在客户端代码中查找 API 端点/路由
- 识别表单输入与参数名
- 理解客户端架构与路由
- 发现认证/存储模式（localStorage、cookie）

## 用法

每次调用一个简单查询。使用与你搜索目标相关的关键词：

**查询模式**：
- API/端点：`"API endpoints"`、`"fetch axios XMLHttpRequest"`
- 表单：`"forms input fields"`、`"form validation"`
- 认证：`"authentication"`、`"localStorage sessionStorage cookie"`
- 文件上传：`"file upload FileReader FormData"`
- 事件：`"event listeners onclick onsubmit"`

## 局限

- **仅客户端**：无服务端源码
- **预索引**：仅此前下载的内容（非实时）
- **只发现不利用**：返回观测到的事实，而非攻击指引

## 说明

- 保持查询简单聚焦（3-5 个关键词）
- 拿到所需信息即停 —— 不要过度查询
- 若反复返回相同结果，那已是全部可用信息
- 实时测试请改用 send_payload
